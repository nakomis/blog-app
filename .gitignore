.DS_Store
*.swp

# The Python scripts (Substack mirror, Bluesky announce, blog ingest) need
# python-substack et al, which are not in the system Python. CI builds this on
# a throwaway runner; locally it is the same one-liner:
#   python3 -m venv .venv && .venv/bin/pip install -r scripts/requirements.txt
.venv/
__pycache__/

# Working plan, not checked in
PLAN.md
